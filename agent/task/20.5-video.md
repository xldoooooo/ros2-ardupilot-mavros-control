# 实现摄像头功能集成

好。按照你的推荐的最简单可靠的路线，实现我说的：“1路 USB 摄像头
1080P 30fps 或 720p 120fps 等
RTSP 推流。必须是RTSP。
同时保存 MP4 （最好是mp4,也可以是mkv或avi）
收到某个信号时保存 JPG
一个内部生产环境使用的场景，就RTSP往局域网某端口推流，然后其他局域网内设备想看就自己拉流。每次录制时间也不会太长，也就不到半小时。在地面站GUI右上角加一个按钮，比如名字叫做：“摄像头配置面板”，点击后出现关于摄像头的相关配置面板，里面包括：摄像头推（拉）流实时画面展示，推流地址（ip,端口，地址），推流画质（分辨率，帧数），编解码方法（h264, mjpeg等），保存格式（mp4, mkv等），视频保存路径，图片保存路径， 开启/关闭摄像头。尽量独立，能单独开启关闭而不影响地面站主体，尽量都实现在video-service目录下。“ mediaMTX你更新到最新稳定版，旧的demo都先留着。

提醒一下，之前用这个型号的摄像头（不是这一颗）测试的时候，h264好像有点问题，帧数不均匀，mjpeg没问题。你留意一下，有这个问题需要说明，但如果没有严重问题则不要修改既定的技术路线）

风格与地面站主GUI协调统一。
注意遵守AGENTS.md