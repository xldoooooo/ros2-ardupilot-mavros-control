# 初始化
1. 阅读理解本项目，完成目录下的MEMORY.md
2. 对地面站ground_station.py(和test_takeoff5.py）进行重构, 要求如下：
  1. 对这个耦合的单文件按照功能进行合理拆分与重构，增强其可维护性。
  2. 把负责发送飞行控制指令的部分，按照功能划分为不同子模块，每个子模块由单个py文件实现，代表一种飞行模式。具体而言：
  起飞/降落模式（起飞/降落按键）
  键盘控制模式（上/下/左/右/前/后/悬停按键）
  航点飞行模式（发送航点指令按键）

  要求：
  2.1 按下某一个按键后，会根据按键对应的飞行模式选择性覆盖当前飞行模式
  2.2

  3. 将仿真初始化流程集成到地面站gui中，实现通过1个“初始化仿真环境”按钮实现全部初始化流程。当前流程包括：1. 运行shfiles/start_sitl.sh; 2.运行shfiles/start_mavros.sh以及设置相关频率 3. （在地面站中）启动rviz
  4. 将实机初始化流程集成到地面站GUI中，实现通过1个“初始化实机环境”按钮实现全部初始化流程。当前流程包括： 1. 启动odin1.sh 2. （在地面站中）设置GPS原点
  5. 重点检查 结束所有进程 的功能是否存在bug, 需确保其能真正彻底结束所有ros等进程，不允许存在任何残余。在结束进程后，应当能在不关闭地面站的情况下再次成功初始化上述两个流程。
  6. 不要修改任何cpp源码。只修改py文件
  7. 将shfiles里的sh脚本功能都集成到地面站中之后，删除这些脚本。
  8. 本次为重构任务，尽量复刻原地面站的功能，不要自行修改功能逻辑；若原地面站程序存在严重bug或低级错误，则一并修改。
  9. 本次任务较为麻烦，生成详细报告
