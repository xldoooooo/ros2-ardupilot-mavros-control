<!-- UQ212 标定单文件化及软链接图标检查记录。 -->
# UQ212 单 Python 文件入口

日期：2026-09-17。用户要求取消 sh/py 分拆，采用与旧 Wasintek 相同的单 py 形式。

## 检查

原两个入口确实为绝对路径软链接；refresh 本机 readlink/namei 确认目标存在，目录权限允许
nvidia 访问，非实际断链。截图紫箭头符合链接标记；叉号/锁可能是查看端目标解析或访问状态，
仅凭截图不能确定具体文件管理器的判定原因。未通过放宽权限猜测性修复。

## 修改

- 仓库删除 run_uq212_calib.sh。
- uq212_cam_calib.py 内置已有 UVC mmap 采集、固定设备/镜头参数和读回逻辑，不再导入
  correction_service 模块。完整保留标定板、算法、交互与结果输出。
- 直接 python3 启动时，在导入第三方库前按 sys.prefix 判断并 exec 项目 .venv。
  不把 venv/python 与系统 python 的软链接同文件误判为已进入 venv。
- refresh 的旧 py 软链接替换为普通0644文件；仅在确认 sh 为软链接后 unlink，未删除其目标
  或其他用户文件。旧 Wasintek 脚本/结果未修改。
- 更新使用说明和 MEMORY；普通文件部署以后需要从仓库复制，不能只 git pull。
- 新增内置相机/镜头参数与生产配置一致性测试，防止后续两处数值漂移。

## 验证和范围

- 开发机相关测试33 passed。
- refresh 从独立标定目录执行 `python3 uq212_cam_calib.py --check-camera` 成功：
  运行环境为主项目 .venv，OpenCV4.6，1920×1080 MJPEG@120，30帧成功，全部12项控制值一致。
  前份报告写13项是计数笔误，实际列表为12项。
- 仍需用户持板完成真实内参标定；未写入生产内参，未进行飞行操作。
- 修改随 main 同步至 refresh；new 飞机仍待同步，原工作区无关修改保留。
