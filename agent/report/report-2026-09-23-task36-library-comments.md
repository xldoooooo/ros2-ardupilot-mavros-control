<!-- 独立规划库公共接口与关键算法注释维护记录；不代表新增运行验收。 -->
# Task36：面向库使用者补充注释

日期：2026-09-23。范围为独立仓库 `dyn_small_obs_avoidance-ros2`。

- 为 SearchConfig 增加单位、逐轴限幅、搜索权重、地图轮换与容量说明。
- 为公开 C++ 接口补充调用顺序、坐标约定、异常、返回值、状态保留/清除语义。
- 明确单实例调用须串行、地图就绪不等于新鲜/覆盖完整、访问节点指针为借用且不得修改。
- 说明初始加速度模式、动态时间索引不等于运动预测、horizon不保证到点或停车。
- 为状态传播、启发式、终端曲线、队列过期条目、边界检查和精确采样补充关键逻辑说明。
- 为ROS节点补充QoS、启动参数、源/接收时钟、速度坐标变换和输入就绪判定说明。
- 为消息字段补充序号生命周期、原始输入时标、耗时范围、多项式导数和采样边界约定。

验证：五个修改文件去除注释及无关空白后与HEAD一致；消息字段/常量保持一致；
`git diff --check`通过。未运行新的性能或飞行测试，不把原有测试结果当作本轮新测量。
仅补充英文注释，没有改动算法、消息结构、依赖或运行配置。
本轮不连接或部署飞机；new/refresh如需这些注释可在后续源码同步时一并更新。
