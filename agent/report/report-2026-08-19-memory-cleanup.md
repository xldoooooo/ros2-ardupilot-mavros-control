# 项目 MEMORY 清理与当前基线整理简报

日期：2026-08-19

## 结论

已把根目录 `MEMORY.md` 从逐任务追加的历史流水账重写为面向后续维护的当前基线文档。
文档由 808 行、84,849 字节精简为 245 行、18,294 字节，保留安全不变量、当前架构、
接口/控制语义、部署事实、验证基线、已知风险和六组关键历史节点。

本任务只修改项目文档，没有修改功能代码、ROS 接口、控制参数、`TODO.md` 或部署脚本；
没有连接、解锁或起飞实机，也没有启动 SITL 或飞行栈。

## 清理原则

- 当前源码、包清单和最近验证结果优先于旧任务描述。
- 同一事实只在当前主题下维护一次，不再按日期重复复制。
- 已被后续版本替代的接口号、UI 默认值、部署状态和测试数字从当前基线中删除。
- 详细实验数字、临时路径、远端一次性 PID/哈希和逐任务过程继续由 `agent/report/` 保存。
- 真实硬件状态采用保守表述：没有最新部署证据时，不把本地源码版本等同于飞机运行版本。

## 主要纠正

- 当前线协议统一为 `3.1`，两个 ROS 包统一为 `3.1.0`；旧 1.0/2.x/3.0 只在必要的历史节点中
  保留，不再作为当前事实。
- 手动操纵默认坐标系统一为当前代码和回归测试确认的“本地 ENU”，删除旧“默认机体坐标”结论。
- 明确根目录 `README.md` 当前存在，删除旧“仓库不维护 README”结论。
- 区分当前仓库 3.1 与真机已知部署历史：截至本次整理，没有报告证明 3.1 已部署并运行于飞机；
  实机连接前必须重新核对 source/install/runtime。
- 统一 `message_rates_configured` 的真实语义：它只表示 MessageInterval ACK，不代表观测到
  100 Hz。
- 统一上位机任务 22、航点异常恢复、摄像头 DRI 兼容和仿真/实机 DDS 隔离的当前行为。

## 保留的长期内容

- 代理不得自行解锁或起飞实机的绝对安全边界。
- 地面站薄客户端、共享接口、唯一机载控制权威和单一姿态 setpoint 发布者边界。
- domain 231/localhost 仿真与 domain 0/subnet 实机隔离，以及 Wi-Fi 被动检测职责。
- 接口 3.1 的租约、TTL、异常、航点参考生成和可靠终态语义。
- 当前 Qt GUI、CSV 航点、RViz 预览、WebSocket 上位机和独立摄像头服务的产品边界。
- sparse 部署、source/install 版本门、systemd 首次校时门和彻底停止入口。
- Humble/Jazzy 混合 DDS、非实时调度、占位避障/机库/媒体接口等未解决风险。
- 当前核心文件的定向重构优先级，明确不进行全仓推倒重写。

## 校验结果

- `MEMORY.md` 标题结构完整，共 245 行。
- 当前接口交叉检查：
  - `ground_station_core/config.py`：`INTERFACE_VERSION = "3.1"`；
  - `src/guided_interfaces/package.xml`：`3.1.0`；
  - `src/onboard_control/package.xml`：`3.1.0`。
- 未再检出“当前接口 2.0”“仓库不维护 README”“默认机体坐标”等已废弃冲突表述。
- 本轮前置项目体检已确认 Python `145 passed`，现有 colcon 结果为 19 tests、0 failures；
  本任务未改功能代码，因此没有重复执行飞行仿真。
- `DEPLOY_UBUNTU_2204.md` 仍有个别接口 3.0 的历史文字，已在新 `MEMORY.md` 中明确标记为
  后续独立文档维护项，本任务按用户范围没有扩改其他文档。

## 变更范围

- 修改：`MEMORY.md`
- 新增：`agent/report/report-2026-08-19-memory-cleanup.md`
- 未修改：功能源码、测试、`TODO.md`、ROS 接口、控制参数、启动/部署脚本和远端主机
