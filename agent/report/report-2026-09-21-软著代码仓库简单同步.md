# 软著代码仓库简单同步执行简报

## 范围与历史基线

- 主项目：`ros2-ardupilot-sitl-hardware`，本次读取至 `7301ac9`。
- 独立软著代码仓库：`/home/nvidia/scq/projects/uav-autonomous-inspection-control`；原始仓库
  `21c8194`、原始 V1.0 标签 `v1.0.0`。对照后确认其主要源码对应主项目 `f037ba4`
  （2026-09-01）。
- 本次仅同步该基线之后的生产代码、接口、相机配置、相关部署入口与 README 中的现行入口。
  未更新软著申报材料、`软件详细开发与使用手册.md`、图片、旧标签，也未迁入测试、实验日志、
  任务记录、专利资料、构建产物或第三方依赖。

## 完成情况

- 同步 97 个历史节点后发生变化的生产文件，包含多 Tag 滑窗修正、独立 UVC 采集、相机档案、
  航点导出和当前位置追加、地面活动提示、飞控热重启与重启恢复、参数读取及时钟恢复等代码。
- 同步了地面/机载脚本目录迁移及机载构建入口迁移；软著仓库继续保留原软件名称、V1.0 品牌、
  Apache-2.0 文件头、LICENSE/NOTICE 与既有作者归属。
- 独立仓库原定不分发测试源码。为使默认入口可构建，机载构建入口明确设置
  `BUILD_TESTING=OFF`，`--verify` 改为依赖检查、构建及隔离 smoke；README 如实说明不含单元测试。
  未因此改变主项目构建或测试流程。

## 验证与限制

- 逐文件核对 97 个生产文件：除软著仓库统一许可证头与软件窗口名称外，与主项目当前文件一致。
- `git diff --check`、Python `compileall`、Shell `bash -n`、项目 XML/URDF 解析通过。
- ROS 2 Jazzy 五包生产构建通过：`guided_interfaces`、`correction_interfaces`、
  `guided_sim`、`correction_service`、`onboard_control`。
- `./scripts/ground/start_ground_all.sh --check-environment` 通过，无飞行会话。
- `./src/onboard_control/deploy/build_onboard_control.sh --verify` 通过；localhost 隔离
  smoke 报告 interface=3.3、FCU 未连接、未解锁、姿态设定值消息为 0。
- 首轮不带 `BUILD_TESTING=OFF` 的构建失败：软著仓库没有随发行分发的 C++ 测试源码。
  补入缺失的 `correction_service/README.md` 后，按软著仓库原定范围调整构建入口并重测通过。
- 未连接实机、未解锁或起飞，也未执行真机部署、完整单元测试或飞行验证；软著历史手册和截图
  保留 V1.0 申报时内容，可能与现在代码入口和功能存在差异。
