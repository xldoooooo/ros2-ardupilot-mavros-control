# Git 大型 MP4 历史清理执行简报

## 任务目标

- 从尚未成功推送的本地提交中移除
  `integration/web_demo/20260709-1525-15.7336731.mp4`。
- 配置仓库忽略所有 `*.mp4` 文件，避免再次误提交大型视频。
- 修订本地未推送提交并同步到远端 `main`。

## 执行情况

- 确认远端 `origin/main` 仍位于 `aaddce7`，本地 `main` 仅领先一个未推送提交。
- 确认工作区中目标视频已删除，其 133,089,621 字节 Git 对象仍在原未推送提交中。
- 在 `.gitignore` 中新增 `*.mp4` 规则。
- 通过修订原提交使大文件不再对远端可达，不保留“先添加、后删除”的可达历史。

## 验证项

- 目标视频不存在于工作区。
- `git check-ignore` 能够命中任意 `.mp4` 路径。
- `origin/main..main` 的可达对象列表中不再包含目标视频。
- 推送结果以本次执行的 Git 输出为准。

## 安全边界

- 本次仅处理本地 Git 历史、忽略规则和文档记录。
- 未连接实机，未执行解锁、起飞或任何飞控操作。
