# 任务 12 执行情况简报：手动操作模式 UI 美化完善

日期：2026-08-09

## 任务结果

已严格按照 `agent/task/12-beautify-2.md` 完成手动操纵区域美化和任务明确要求的坐标/灵敏度行为。没有实现此前讨论但未写入任务文件的手动输入锁、ARM/DISARM、RTL、自动回中、输入超时、游戏手柄或其他扩展功能。

1. 左右两组十字按键分别增加圆角方形摇杆底盘，底盘中央带回中圆盘和实时偏移点。
2. 鼠标按住方向键或键盘快捷键触发时，对应按钮使用蓝色高亮，中央偏移点同步向该方向移动；释放或短脉冲结束后回中。
3. 左摇杆与右摇杆中央分别增加独立灵敏度三挡：低 `0.5×`、中 `1.0×`、高 `2.0×`。倍率只作用于所属摇杆的单次增量。
4. 增加“机体坐标 / 本地 ENU”选择：
   - 机体坐标为默认模式；右摇杆 XY 增量按最新机头偏航角旋转到机载协议既有的本地 ENU；
   - 本地 ENU 模式保持原固定 X/Y 增量；
   - 鼠标按钮和键盘快捷键共用同一命令生成入口，避免坐标或灵敏度语义不一致。
5. 手动区顶部始终显示当前坐标系、控制权和最近一条实际发送的手动指令年龄；运动指令和“制动并悬停”均会更新时间。
6. 原五行等宽工程文本改为五个大数字摘要：实际高度、实际航向、目标水平速度、目标升降速度和目标偏航速率。
7. 完整 ENU XYZ、目标速度/位姿、控制频率、jitter、deadline miss 与安全门控移入“工程信息”，默认折叠，展开后保持完整可读宽度。
8. 悬停按钮文案改为“制动并悬停  SPACE”。
9. 左摇杆贴近卡片左侧、右摇杆贴近卡片右侧，减少外侧无效留白。
10. 上排两张卡片内部显式顶对齐，“飞行动作”标题现与“环境与连接”标题使用相同左上角位置。

## 修改文件

- `ground_station_core/qt_ui/operations_panel.py`
  - 新增摇杆偏移绘制组件、双底盘、灵敏度选择、坐标转换、按压反馈、常驻状态、大数字摘要与折叠工程信息。
- `ground_station_core/qt_ui/main_window.py`
  - 键盘快捷键改为调用操作面板的统一命令生成入口；实际发送运动/悬停后记录最近指令时间。
- `ground_station_core/qt_ui/theme.py`
  - 增加底盘、按压高亮、状态 chip、摘要卡、折叠入口和紧凑灵敏度选择器样式。
- `tests/test_qt_gui.py`
  - 覆盖标题顶对齐、底盘与按键位置、按压/回中反馈、默认折叠、摘要和常驻状态；
  - 覆盖 90° 偏航下机体坐标转换、ENU 直通、左右独立倍率及键盘/鼠标统一语义。
- `MEMORY.md`
  - 记录任务 12 的最终 UI、输入语义和验证基线。

## 验证结果

```text
Qt 全量回归：
24 passed in 94.87s

加载 /opt/ros/jazzy/setup.bash 与 install/setup.bash 后 Python 全量：
46 passed in 96.56s

任务 12 最终定向回归：
2 passed, 22 deselected in 1.83s

compileall：通过
flake8 E9/F63/F7/F82：通过
git diff --check（本任务代码/测试文件）：通过
新增差异行长度检查（≤88）：通过
```

视觉证据：

- `agent/codex/task12-beautify-2-main.png`：1600×920 默认折叠状态；
- `agent/codex/task12-beautify-2-active.png`：右摇杆 I 键高亮与偏移反馈；
- `agent/codex/task12-beautify-2-expanded.png`：工程信息展开且字段完整；
- `agent/codex/task12-beautify-2-minimum.png`：1180×700 最小窗口，左右主栏同时保留，左侧通过既有纵向滚动访问下方内容。

## 控制范围与实现说明

- 坐标转换和灵敏度是任务要求所必需的 GUI 命令生成逻辑；没有修改 `MotionIntent` 消息、地面站 ROS 传输、机载 C++ 状态机或 PD+DOB 控制器。
- 机体坐标转换只旋转本次右摇杆 XY 增量，使用最近一次 `VehicleSnapshot.yaw`；机载端仍接收并累计本地 ENU 增量。
- 左摇杆倍率同时作用于升降增量和偏航增量；右摇杆倍率作用于前后/左右平移增量。默认 `1.0×` 保持任务 11 的 `0.2` 基准增量。
- ROS/C++ 源码与接口没有变化，因此未执行无关的 colcon 重建。

## 安全说明

- 未启动 SITL、MAVROS、机载节点或 RViz。
- 未连接实机、未申请控制租约、未写入飞控原点或消息频率。
- 未执行解锁、起飞、降落、航点或任何飞行命令。
- 未增加飞行器武装拨杆、手动操纵输入锁或紧急停桨入口。

## 已知非本任务问题

仓库全局 `git diff --check` 仍会报告任务开始前已有的 `TODO.md:57: new blank line at EOF`。本任务未获授权修改 `TODO.md`，因此只对任务涉及的代码和测试文件执行并通过差异检查。
