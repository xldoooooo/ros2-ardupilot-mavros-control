# control.yaml 中文参数注释补全简报

## 1. 修改内容

- 日期：2026-08-13。
- 将 `src/onboard_control/config/control.yaml` 原有英文分组说明全部改为中文。
- 保留每组参数前的中文总述，并为全部 67 个 ROS 参数逐行增加中文行尾注释。
- 行尾注释说明了参数用途、适用控制模式、物理单位或判定语义；速度、加速度、加加速度、时间、
  角度、频率和无量纲量均明确标注。
- 参数名称、参数数量、参数顺序和参数值均未改变，没有调整任何控制效果或安全阈值。

## 2. 验证结果

- 使用项目 Python 环境和 PyYAML 解析修改前后的文件，确认 67 个参数键值完全一致。
- 自动检查全部 67 行参数均包含同行注释，没有遗漏。
- `git diff --check` 通过。
- 在 ROS domain 232、仅本机发现和隔离命名空间中启动 `onboard_control_node`，节点成功读取参数并
  报告接口 3.0、控制频率 100 Hz；3 秒后由 `timeout` 正常终止。
- 本次没有启动 SITL，没有连接、解锁或起飞实机，也没有发送任何飞行命令。
