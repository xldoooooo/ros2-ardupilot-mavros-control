# Task36 可配置盲区过滤

## 交付

- 独立库提交 `e8472ac`；新增 `cloud.blind_radius`（默认0关闭）、
  `cloud.blind_origin_offset`（默认零，里程计child坐标系米制偏移）、
  `cloud.blind_pose_tolerance`（默认0.05秒）。启动后需重启应用修改。
- 使用最近源时间戳里程计、最多256条历史；旋转球心偏移后，在世界系对当前帧实施三维球形过滤。
  保留严格半径边界，累计前执行，不按当前位姿挖除旧地图；不匹配清图并报告状态。
- 全部滤空为NO_MAP，非有限点仍按原规则拒绝；新增 `/filtered_cloud` 当前帧XYZ供RViz比较。
- Odin入口显式 `blind_radius:=0.5` 启用；该启动参数覆盖YAML半径，默认仍关闭。
  三维球形不同于上游FAST_LIO水平距离盲区，不识别机体，球内真实障碍同样被忽略。

## 验证

- 本机构建通过，12项核心测试、2项参数化DDS进程测试通过；colcon汇总16项含包装项。
- 回归覆盖默认关闭、平移原点、旋转安装偏移、外部障碍保留、全滤空清图、源时钟不匹配清图，
  以及原有路径/失败/过期/输入合法性验证。
- 首轮新增测试受旧成功结果影响导致地图点数断言失败；加入帧失配清图和目标修订/终点核对后通过，
  随后增加偏移旋转覆盖并再次通过。没有降低功能验收标准。
- new（192.168.112.169）定点更新并原生构建path_planning，重跑2项DDS进程测试通过。
  核心未修改，机上历史核心测试结果未当作本次重跑；没有重复ASan或长时间性能测试。
- 实时盲区0.5m，5帧raw分别29288/29277/29349/29311/29332点，过滤分别
  2198/2139/2207/2177/2231点；被动检查同时间戳里程计与点云差0秒，最近保留点为
  0.50412/0.50802/0.50450/0.50274/0.50365m，过滤实际生效。
- 目标(4,0,1)仍返回NO_PATH，约35130累计点，无有效轨迹；未改碰撞半径或目标伪造成功。

## 部署与限制

- 旧现场文件备份在new `/home/nvidia/task36-backups/blind-20260924/`。
- 仅停止用户独立planner并启动新版本，当前launch PID21081，日志 `/tmp/task36-odin-blind.log`。
  运行参数 `blind_radius=0.5` 已读回；保留用户原目标以供继续观察。
- new更新wrapper、config、launch和测试；核心仍初始源码，未整体同步git历史；refresh未部署。
- 默认零偏移表示以IMU里程计原点为球心，不代表雷达与IMU外参已标定；时间匹配不做插值。
- 飞控/传感器服务未改动，未解锁、未起飞，无飞行控制输出。本功能验证不代表实飞避障成功。
- 本机过程日志位于agent/codex/task36/blind-*.log。
