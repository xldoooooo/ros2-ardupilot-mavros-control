# Odin 实时规划启动配置修正

## 修改与部署

- 独立库新增 `path_planning/launch/odin.launch.py`，提交 `47445e2`。
- 默认订阅 `/odin1/cloud_slam`、`/odin1/odometry`、`/goal`；使用 odom 坐标系和 receive 时钟模式。
- 搜索 z 下界改为 -2m 以容纳启动原点附近的台架位姿；其余通用参数和碰撞半径不变。
- 使用说明补充启动、目标发布和结果检查命令。
- 192.168.112.169 定点增加上述 launch 并通过 path_planning 原生 colcon 构建；未整体更新旧版本源码。
- 未修改飞控服务，没有解锁、起飞或向飞行控制接口发送命令。refresh 未同步。

## 验证与限制

- launch Python 语法及 git diff --check 通过，远端实际 launch 启动和正常结束通过。
- 使用独立诊断目标话题发布 odom 帧目标 (4,0,1)。实际约16431地图点，状态3 NO_PATH，
  `/kino_path` 持续发布空 poses；此前 FRAME_MISMATCH 和起点越界 INVALID_INPUT 已排除。
- 被动单帧检查：当前位置约(-0.00287,-0.00076,-0.003981)m，最近原始点距离0.10198m；
  目标最近原始点距离0.09622m。两者均低于约0.648m有效安全半径，支持端点碰撞拒绝结论。
- 未删除近身点或缩小安全距离，没有伪造有效规划结果；成功实景轨迹仍未验证。
- receive 模式仅保证接收端新鲜度，不能验证传感器端延迟。-2m 下界是搜索范围而非物理地面。
- 本次为启动配置修改，不重复完整核心/ASan/性能测试；诊断节点已退出，用户可按文档自行启动。
