<!-- 独立规划器配置说明和具名常量整理报告，记录本轮验证与部署边界。 -->
# Task36：配置参数说明与 magic number 整理

日期：2026-09-23。修改范围为独立仓库 `dyn_small_obs_avoidance-ros2`。

- 为planner.yaml全部24个参数补充单位、范围、作用、模式限制和主要调参影响。
- 标明碰撞半径补偿公式、双树按帧而非时间轮换、逐轴动力学限幅及启动时读取配置的约定。
- 将搜索采样比例、近目标阈值、shot时长重试、启发式时间/代价常量、数值容差和资源上限
  提取为带说明的文件内constexpr常量。
- 为ROS时间容差、四元数模长容差、频率配置上限和PointCloud2字段字节数命名。
- 保留多项式数学系数及结构下标的直接表达；不增加无必要的运行时参数或全局常量框架。
- 明确上游tie_breaker实际参与的倍率为2.0001，未借注释整理改动启发式行为。
- 使用指南增加运行配置与内部实现常量的维护边界。

验证：YAML解析后与修改前完全一致，共24项配置；本机Jazzy Release两包构建成功，
现有12项核心回归及ROS2进程测试通过，ament汇总15项（含2个CTest包装项）、0失败。
`git diff --check`通过。代码重构保留原数值和比较逻辑，没有新增算法行为。

本轮未连接/部署new或refresh，也未重新进行性能、ASan或实飞测试。
代码和文档按main提交推送；过程日志在`agent/codex/task36/constants-*.log`。
