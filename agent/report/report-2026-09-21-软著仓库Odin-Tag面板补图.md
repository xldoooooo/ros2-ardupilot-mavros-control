# 软著仓库 Odin-Tag 面板补图简报

## 缘由与范围

- 前一次截图更新只检查了软著仓库已有的图片；独立 AprilTag-Odin 面板虽然自历史快照后
  明显改版，却没有单独图片，因而被遗漏。本次按用户提醒补图。
- 在软著代码仓库新增 `assets/AprilTag-Odin修正面板.png`，并在仓库 README 增加图片与
  离线状态说明。未修改原软著申报材料、历史手册、其他截图或生产代码。

## 截图核验

- 使用软著仓库当前已构建的 ROS 2 Jazzy 接口、项目 `.venv` 与 Qt offscreen 平台，实际运行
  `CorrectionPanelWindow`；ROS 域为仅本机发现的 231，与实机域隔离。
- 截图尺寸 1180×1050，显示五个操作按钮、服务端滑窗、候选质量、extnav 状态、各段位姿对照
  和最近任务结果。目视核对无截断或缺字。
- 截图中的修正服务和位置数据如实显示“不可用/过期”。本次未调用 first/next、apply、clear
  或任何飞行命令；未连接实机、未解锁或起飞。截图仅说明当前 UI 布局，不代表校准成功。
