# Matching values on UAV and ground station enable ROS 2 discovery on one trusted LAN.
ROS_DOMAIN_ID=42
ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET

# If multicast discovery is unreliable, list the ground-station address explicitly.
# ROS_STATIC_PEERS=192.168.1.20
