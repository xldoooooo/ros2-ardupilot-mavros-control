# 下视相机固定 V4L2 参数；收到首帧且视频流稳定后，按文件顺序写入并最终逐项读回。
# 手动曝光、曝光时间和增益必须优先，避免设备开流恢复默认值覆盖预设。
[controls]
auto_exposure = 1
exposure_time_absolute = 25
gain = 240
brightness = 10
contrast = 6
saturation = 6
hue = 0
sharpness = 6
power_line_frequency = 1
zoom_absolute = 10
