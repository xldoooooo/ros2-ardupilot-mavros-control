# Wasintek 下视镜头控制档案；数值和写入顺序复制自 2026-09-16 的活动配置。
# 本文件只用于恢复旧相机，不会被 correction_service 自动加载。
[controls]
auto_exposure = 1
exposure_time_absolute = 50
gain = 200
brightness = 6
contrast = 6
saturation = 6
hue = 0
sharpness = 6
power_line_frequency = 1
zoom_absolute = 10
