# TODO

## 控制算法

- [ ] 修复平移指令起步晃动：增加速度参考加速度/jerk 限制、悬停到运动的无扰切换，并处理键盘自动重复。
- [ ] 需要调控制参数至少恢复之前的效果
- [ ] 将发布航点的飞行模式 进行差值处理，运动平滑。 二阶命令滤波器或梯形速度



## 地面站/机载服务

- [x] 删除终端 按钮 和 文件按钮里的开启终端功能，终端集成到日志输出栏，类似vscode integrated terminal(不做)
- [x] mavros启动等的消息，等级从info降为debug
- [x] 未启动仿真或实机连接时，禁用航点组件
- [x] 点击启动仿真或实机连接按钮后，在其启动初始化流程完成之前，在主界面中间弹出一个不可拖动的提示框并锁定整个主界面，使整个主界面变灰色。（日志部分不受影响）（没有必要）
- [x] 当前启动地面站比较缓慢。检查是否存在优化空间，例如并行启动多个没有前后依赖的程序。主要瓶颈在cpu，只能优化不到20%
- [ ] 增加一个（或改进现有的）更直观的展示当前状态的bar或进度条，展示启动进度，当前已完成的步骤，未完成的步骤，就像
- [x] 调研 仿真与实机连接 能否开启(不做)
- [x] 局域网通讯测试
- [x] 机载服务独立运行测试
- [x] 日志完整性合理性审查 基本满足 完全足够当前阶段
- [x] 消息收发是否成功 加强检验逻辑 握手机制 日志可靠性 基本满足 完全足够当前阶段
- [x] 手动飞行意图 重构为美国手摇杆布局(左：WASD 上升下降左转右转； 右IJKL 前进后退左移右移)
- [x] 手动飞行的按钮/键盘操作逻辑，从增量改成维持（手柄效果）（不做）
- [x] 优化界面拖拽性能
- [ ] 将摄像头画面嵌入地面站GUI或单独的功能模块
- [ ] PID/DO热调参入口
- [ ] 飞控参数传入入口
- [x] 手动操作的逻辑，参考系选选项
- [x] 鼠标悬停在左右摇杆提示 速度/角度改变量 提示信息
- [x] 下方详细信息显示点击某按键后具体的增量大小
- [x] 调查并解决此新出现的警告

    15:24:39.873 [INFO ] [remote-rosout:onboard_control_node] 无法同时读取 GUID_OPTIONS 与 MOT_THST_HOVER，姿态/推力控制保持禁用
    15:24:43.924 [WARN ] [remote-rosout:mavros.param] PR: Param STAT_RUNTIME (65535/1246): 228926 different index: 1201/1246

    已解决，原因是MAVROS 连接后约 30 秒才完成完整参数表同步。旧机载节点过早且每 5 秒查询一次，导致暂时读不到两项参数并重复打印警告。没有实际问题

- [x] 一键集成启动机载服务；一键集成启动地面站服务
- [x] 飞机开机自启服务； headless rviz不启动
- [x] 地面站重启； 各种重启/掉线重联测试
- [x] 检查通讯频率，重点排查导致阻塞的原因 buffer exceed 导致 ros2 topic echo异常
      通讯频率健康：飞机-地面站 mavros status：10Hz 地面站-飞机 heartbeat：5hz； 地面站其余指令 均为事件驱动，没有高频反复发送
      echo异常应该是 /mavros/set_message_interval 没有设置导致的，旧的odin.sh会手动设置三个频率。
      为什么有时候能正常启动，原因可能是如果此前地面站、旧 `odin.sh` 或其他路径成功配置
      了 message interval，或者飞控按 `SRx_*` stream 配置发送了 message 32，同一个 `topic echo` 即使先
      打印一次 buffer error，也仍可能随后收到 pose。
- [x] 当飞机机载服务被关闭时，地面站是否继续显示已连接飞机的界面，还是主动结束连接逻辑，恢复到初始化界面
      已解决：不恢复到初始化界面，保持连接ui但禁用起飞等按钮；因为在这种状态下，只需重启机载服务，则地面站可以自动重连实机
- [x] 修复断开连接后依然与飞机保持通讯的bug（连接一次后断开，GUI界面恢复未初始化的状态，但实际上并没有彻底断开，飞机移动的时候地面站上仍然会实时显示飞机的高度/偏航等）
- [x] 修复仿真与实机同时存在时的冲突问题. 飞行先使用DOMAIN 0.
- [ ] 深入研究一下sequence size exceeds remaining buffe 到底是什么消息导致的
- [x] 航点的三维预览图；飞机实时位置 这个 可能与rviz重复 需调研。 结论：利用rviz是比较好的方案
- [x] 2204 地面站系统兼容
- [x] 实时飞机电池状态
- [x] 显示飞控状态


## AP

- [ ] 删除或禁用起飞时的GPS原点强制性依赖（已经查明，做到这一点不需要修改ap源码，只需要修改其配置参数，强制使用GPS原点实际上是本项目的问题
      PD+DOB 本地起飞 或 Loiter 原生起飞 → Guided 接管
      还需确认从Loiter切换到Guided的时候怎么保证是无缝的
- [ ] 修改/调试其集成的EKF算法
- [x] 实现在不重启机载计算机的情况下完成ap飞控完全重启 （结论：至多与在飞控界面点击重启一样。更加彻底的重启要改造电气连接）
- [x] 0.3m高度误差 排查（未解决但修复地面站后不再出现）
- [ ] 实现从地面站传入起飞/降落速度参数的功能


## 主要节点

- [x] 重构原ground_station.py为可维护文件结构
- [x] 重构整体逻辑，将地面站与机载控制算法解耦
- [x] 重构Tkinter为PySide6 GUI实现方法
- [x] 较为美观的 可用的 无bug/少bug 的初版地面站GUI程序
- [x] 在地面站集成启动/终止完整服务功能，集成日志反馈等信息
- [x] 将独立的机载服务部分部署到机载计算机并完成本地环境适配编译
- [x] 完成机载计算机+机载服务 <-> 地面站+指令输入 不含飞控的局域网通讯测试
- [x] 完成真机包含飞控的完整通讯链路验证
- [x] 完成地面站-真机试飞验证
- [x] 完成机载服务-地面站任一方掉线另一方热重联检验; 隔绝本地仿真 与实机连接互不影响
- [ ] 将机载odin等部署方法的相关必要文件统合整理进仓库，形成可复现可移植的文档
- [ ] 控制算法调参，达到不低于初代版本的效果
- [ ] 删减调整各日志与文本内容，进行产品化润色
- [ ] 演示试飞
- [ ] 实现甲方地面站通讯协议对接

## 需要协助

- [x] 飞机螺旋桨拆除与地面供电（不拆）
- [ ] 修改AP源码涉及编译
- [ ] 甲方通讯协议非常模糊，缺少细节，协议文档仅草稿demo级，无法工程化落地

  ![image-20260809024728463](/home/nvidia/scq/projects/ros2-ardupilot-sitl-hardware/assets/image-20260809024728463.png)


## 远期任务

- [ ] 实现甲方地面站的功能
- [ ] 实现航点飞行任务的避障模式
- [ ] websocket/其他消息机制 改造 -> 不再依赖ros2
- [ ] webui electron
- [ ] 三维点云建图
- [ ] 避障 曲线跟踪
- [ ] 进圈判断
- [ ] UWB-EKF
- [ ] 多标签界面，集成rviz等
- [ ] 多机适配？
- [ ] 产品级日志系统 更全面的校验握手逻辑
- [ ] 工程化注释
- [ ] 改飞控电气连接，实现远程冷重启

